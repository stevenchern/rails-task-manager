<div>
  <h1>My tasks</h1>
  <% @tasks.each do |task| %>
    <% if task.completed %>
    <h3><%= '✅ '%>
    <% else %>
    <h3><%= '❌ ' %>
    <% end %>
    <%= link_to task.title, task_path(task) %></h3>
  <% end %>
</div>
<div>
  <%= link_to "➕ Add a 🆕 task", tasks_new_path %>
</div>
